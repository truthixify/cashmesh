{
  "name": "cashmesh",
  "version": "0.0.0",
  "private": true,
  "description": "Merchant acquiring and Stellar settlement for multi-operator Cashu e-cash.",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "dev": "pnpm --parallel --filter @cashmesh/acquirer-api --filter @cashmesh/merchant-console dev",
    "dev:api": "pnpm --filter @cashmesh/acquirer-api dev",
    "dev:merchant": "pnpm --filter @cashmesh/merchant-console dev",
    "format": "biome check --write apps services packages && cargo fmt --all",
    "lint": "biome check apps services packages && cargo fmt --all -- --check && cargo clippy --workspace --all-targets --all-features -- -D warnings",
    "test": "pnpm test:js && pnpm test:rust",
    "test:e2e": "pnpm --filter @cashmesh/merchant-console test:e2e",
    "test:js": "pnpm -r --if-present test",
    "test:rust": "cargo test --workspace",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.11",
    "@playwright/test": "1.62.1",
    "@types/node": "24.13.3",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
